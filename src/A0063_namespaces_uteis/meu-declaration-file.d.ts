declare namespace _ {
    declare interface LoDashStatic {
        mul(array: number[]): number;
        bla: string;
    }
}

declare namespace NodeJS {
    interface global {
        MINHAGLOBAL: string;
    }
}
